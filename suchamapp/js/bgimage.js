const images = "https://source.unsplash.com/random";
//const choseImage = Math.floor(Math.random()*images.length);
const bgImage = "url(https://source.unsplash.com/random)"

document.body.style.backgroundImage = bgImage;
console.dir(bgImage);